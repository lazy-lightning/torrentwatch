Add a "processing" div that displays while the torrent list is being calculated
Possibly update that div while calculating

lastRSS still writes to the cache with bad fetch
do the dltorrent link in hidden iframe
change mode=dltorrent to output little or no info.

